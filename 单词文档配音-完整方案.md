# 单词文档配音—完整方案

## 一、项目概述

“单词文档配音”面向拥有静态词汇 PDF、但缺少配套发音的语言学习者。软件把规则词表提取成结构化词条，为每条内容生成独立语音，并支持搜索、校对、逐词播放和 Anki 导出。

它不是从第一页连续朗读到最后一页的普通 PDF 阅读器。核心要求是让“序号、单词、注音、释义、页码、异常标记和音频”始终保持一一对应。

## 二、当前 v0.3.1 范围

目标输入为 `英语四级词汇完整版带音标-乱序版--共107页.pdf`：

- 文本型 PDF，无需 OCR；
- 四列表格：`序号 | 单词 | 注音 | 释义`；
- 107 页、4450 条记录；
- 序号 1—4450 连续。

v0.3.1 提供：

1. 只读分析 PDF 并提取完整词表；
2. 标记缺少注音、重复拼写、多读音和可疑源数据；
3. 搜索、筛选、编辑和逐词试听；
4. 使用本地 Kokoro ONNX 生成英语 WAV；
5. 先生成 30 词样本，确认后可生成全部；
6. 用“只看已有音频”检查当前已完成内容，并可直接打开 WAV 文件夹；
7. 在全部生成前先导出已有音频的 Anki `.apkg`，全部完成后再严格导出全量卡组；
8. 提供无需系统 Python 的 Windows 便携版；
9. 声音或语速改变后自动重新生成不匹配的旧缓存，并显示中文友好声音名称；
10. 按数字对序号进行升序、降序排列。
11. 动漫治愈风界面，小新、风间、正南、阿呆、妮妮与两枚原创学习贴纸随机轮换，包含 12 条非连续重复的温暖短句；
12. 适配 Windows 150% 显示缩放，首屏完整显示摘要、设置、词表和所有操作。
13. 珊瑚粉可爱小书程序图标，以及升降序一致的稳定表格列宽。

## 三、核心流程

```text
选择 PDF
  → 后台逐页提取四列表格
  → 校验 4450 条与异常数据
  → 写入本地 SQLite
  → 搜索 / 异常筛选 / 已有音频筛选 / 编辑 / 逐词试听
  → 生成 30 词样本或生成全部
  → 导出已有音频，或完成后导出全量 Anki 卡组
```

## 四、技术结构

| 模块 | 技术 | 职责 |
|---|---|---|
| 桌面界面 | Qt / PySide6 Essentials | 非阻塞导入、词表、编辑、进度和操作反馈 |
| PDF 提取 | pdfplumber | 按页面表格提取四列数据 |
| 本地数据 | SQLite | 原始字段、人工修改、异常和音频状态 |
| 本地发音 | kokoro-onnx | 离线生成 WAV，按词条稳定缓存 |
| 音素转换 | eSpeak NG | 为 Kokoro 提供英语音素，兼容 Windows 中文路径 |
| Anki 导出 | genanki | 稳定卡片 ID 和实际媒体打包 |
| Windows 成品 | PyInstaller | 便携目录版，内置模型和运行库 |

正式代码位于 `src/word_voice/`：

- `extractor.py`：PDF 提取和结构校验；
- `storage.py`：SQLite 与人工修改保护；
- `tts.py`：本地发音、缓存和文件命名；
- `anki_export.py`：卡片与媒体打包；
- `app.py`：桌面界面和后台任务；
- `cli.py`：命令行入口。

## 五、关键产品决策

- 原始 PDF 永远只读，不覆盖、不提交到 Git。
- 使用 PDF 序号作为词条唯一标识；同形词不能共用音频。
- 音频文件名包含序号，例如两个 `present` 仍对应不同文件。
- 人工修改保留原字段，重新导入时不得静默覆盖。
- 生成失败必须可见、可重试；有效缓存不重复生成。
- 本地处理优先，不上传 PDF、词表或音频。

## 六、已知源数据异常

- 序号 583 的 `a.m` 缺少注音。
- `present` 出现两次，词性与读音不同。
- `forbid` 出现两次，其中一条疑似应为 `forbidden`，保留源数据并提示人工确认。
- 部分词条含多个读音，默认采用主要读音并进入异常筛选。

## 七、当前验证结果

- 真实目标 PDF：107 页、4450 条、序号完整；
- 修复后的便携版分析耗时约 14.9 秒；
- 包含真实目标 PDF 的完整 22 项自动测试通过；
- 构建后的成品程序必须实际生成 Sarah、Adam、慢速和快速 24 kHz 单声道 WAV，并确认参数产生真实差异；
- 30 个代表词已生成有效的 24 kHz 单声道 WAV；
- 30 词测试 Anki 卡组已经成功打包；
- v0.2 真实迁移测试复制 1104 条已有音频，并成功导出约 45.9 MB 的部分 Anki 卡组；
- Windows 便携版约 367 MB，内置离线模型。
- v0.3.0 源码界面在 150% 显示缩放下完整加载 107 页、4450 条、89 条需留意和 1104 条已有音频。
- v0.3.0 便携版通过多声音、多语速真实 WAV 门禁，打包版最终界面截图保存在 `assets/screenshots/v0.3.0-main.png`。
- v0.3.1 删除耳机小恐龙和未采用的角色草稿，加入按参考图生成的角色贴纸、小书图标和升降序列宽回归检查。

## 八、仓库分类

根目录采用 Obsidian 项目导航方式：

- `README.md`：项目首页；
- `单词文档配音-完整方案.md`：总方案；
- `单词文档配音方案文档索引.md`：统一文档入口；
- `单词文档配音项目交接文档-2026-07-17.md`：当前接手基线；
- `更新日志.md`：中文时间线。

工程内容继续按职责分类：

- `spec/` 当前交付规格；
- `docs/` 产品、实施和经验文档；
- `assets/` 可提交资源及使用规则；
- `scenes/` 前端场景与审美说明；
- `scripts/` 安装、调试、构建和提取入口；
- `src/` 正式代码；
- `tests/` 自动测试；
- `packaging/` Windows 打包配置；
- `dist/` 本地便携版，不提交 Git。

## 九、暂不纳入 v0.3.1

- 扫描 PDF OCR 和任意复杂版式；
- 直接在原 PDF 中嵌入音频；
- 手机应用、账户、云同步和多人协作；
- 例句生成、例句配音和声音克隆；
- 自动改写教材中的全部拼写、音标和释义。

## 十、下一步

1. 用“只看已有音频”抽查当前生成结果；
2. 导出已有音频卡组并在安装 Anki 后完成首次导入验证；
3. 针对 `present`、`forbid` 等异常词人工校对；
4. 继续生成完整 4450 词音频；
5. 随机抽查至少 50 条并导出正式全量卡组。

## 十一、版本更新约定

v0.2.0 按要求与 v0.1.1 独立保存，并在首次打开 PDF 时复制已有结果。从 v0.2.1 开始采用普通升级：更新版本号、代码和两份更新日志，不再默认复制整套用户数据。详见 [[docs/versioning-and-data]]。

当前交付边界与验收细节以 [[spec/current]] 为准。
